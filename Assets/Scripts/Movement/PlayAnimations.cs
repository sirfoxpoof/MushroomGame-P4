using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayAnimations : MonoBehaviour
{
  /*  public Animator playerAnimator;
    public Rigidbody rb;
    public PlayerMovement playerMovement;
    private float normalSpeed, sprintSpeed;

    void Start()
    {
        rb = GetComponent<Rigidbody>();
        playerMovement = GetComponent<PlayerMovement>();

        //speed goedzetten
        normalSpeed = playerMovement.moveSpeed;
        //sprintSpeed = playerMovement
    }

    // Update is called once per frame
    void Update()
    {
        if(rb.velocity.x > 0 || rb.velocity.y >0)
        {
            print("heeeeyy");
            if(playerMovement.moveSpeed > 1)
            {
               print("nu moet je sprinten");
                playerAnimator.SetFloat("Speed", rb.velocity.magnitude);
            }
            else
            {
                print("nu moet je lopen");
                playerAnimator.SetFloat("Speed", 1);
            }
        }
    }*/
}
